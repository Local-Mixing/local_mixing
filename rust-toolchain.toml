[toolchain]
# Keep the compiler fixed while reorganizing the codebase.
channel = "1.97.1"
profile = "minimal"
components = ["rustfmt", "clippy"]
# Install rust-analyzer separately when needed by an editor.
