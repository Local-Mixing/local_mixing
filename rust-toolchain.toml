[toolchain]
# Pin the toolchain so every machine (incl. the remote server) builds with the
# same compiler. edition 2024 requires rustc >= 1.85.
# 1.97.1 verified 2026-08-18: fmt/clippy/check all clean, golden outputs stable.
channel = "1.97.1"
components = ["rust-analyzer", "rustfmt", "clippy"]
